#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints its name
 * @argc: number of inputs (at least 1)
 * @argv: vector of args
 * Return: (0)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
