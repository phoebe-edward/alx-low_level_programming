#include <stdlib.h>
#include <stdio.h>
/**
 * main - print mul
 * @argc: number of args
 * @argv: args
 * Return: (0 or 1)
 */
int main(int argc, char *argv[])
{
	int mul;

	if (argc != 3)
	{
		(void)mul;
		printf("Error/n");
		return (1);
	}
	else
	{
		mul = atoi(argv[1]) * atoi(argv[2]);
		printf("%d\n", mul);
	}
	return (0);
}
