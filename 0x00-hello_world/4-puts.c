#include <stdio.h>
/**
 * main - entry point
 * prints a string to the std output with a new line at the end
 *
 * Return: 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
