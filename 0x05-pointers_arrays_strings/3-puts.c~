#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string
 *@str: character array pointer
 */

void _puts(char *str)
{
	int i;

	i = 0;

	while (str[i] != '\0')
	{
		putchar(str[i]);
		i++;
	}
	putchar('\n');
}
